# コスメのホンネ 仕様書 v1.0

**ミッション**: メーカーへの忖度を排除し、ユーザーの実利(肌・髪・お財布)を最大化する。バックエンド・認証・APIキーを持たない、完全クライアントサイドの美容分析ツール。

## 1. 概要

成分表を貼り付けるだけで、広告文言に頼らない客観的な採点を返す美容分析ツール。

**提供価値**
- 成分ホンネ分析:パッケージのコピーではなく成分表そのものを評価
- コスパの可視化:価格のうち「中身」と「ブランド料」を分離して提示
- パーソナライズ:肌質・髪質・悩みを登録すると分析コメントと商品提案に反映

**非対象**:医療的アドバイス、皮膚科診断の代替、特定ブランドの広告

## 2. 対象ユーザー・利用シーン

- 店頭やECで成分表を見て、その場で「買う価値」を判断したい人
- 自分の肌質・髪質に合う市販品を成分ベースで知りたい人
- 高価格帯コスメの中身に納得してから買いたい人
- 手持ちコスメと新しいアイテムの併用が大丈夫か確認したい人

## 3. スコープ・技術方針

フロントエンドのみで完結。唯一の外部通信は天気取得(Open-Meteo、匿名・キー不要)。永続化はブラウザの`localStorage`のみで、サーバー同期は行わない。

| 領域 | 採用技術 |
|---|---|
| フレームワーク | React 18 + Vite + TypeScript(strict) |
| スタイリング | Tailwind CSS + shadcn/ui(Radix UI) |
| 状態管理 | Zustand(persist middlewareでlocalStorage保存) |
| ルーティング | React Router v6 |
| 外部API | Open-Meteo(天気予報、キー不要)のみ |
| 認証・DB | なし |

## 4. 情報構成

下部固定タブは5つ(ホーム/成分/コスパ/ポーチ/わたし)。利用頻度の低い「成分組み合わせ事典」はタブに置かず、ホームとポーチからのリンク導線でアクセスする。

| Path | 画面 | 目的 |
|---|---|---|
| `/` | Home | 今日・週間の肌アドバイス、各機能への導線 |
| `/check` | CheckPage | 成分ホンネ分析(メイン機能) |
| `/cost` | CostPage | コスパ診断 |
| `/pouch` | PouchPage | 手持ちコスメの登録・相性チェック |
| `/profile` | ProfilePage | 肌質・髪質プロフィールとおすすめ(2タブ) |
| `/guide` | GuidePage | 成分組み合わせ事典 |
| `*` | NotFoundPage | 404 |

## 5. 機能仕様

### 5.1 成分ホンネ分析
- **目的**: 成分表示テキストから広告に依存しない客観採点を返す
- **入力**: 自由記述の成分表テキスト、または5種のサンプル
- **処理**:
  1. `tokenize()` — 「成分:」等の接頭辞除去→読点/カンマ/改行で分割→全角英数記号の半角化
  2. `matchToken()` — 辞書(約110件)に対し完全一致→部分一致(3文字以上)→パターン推定(語尾ルール8種)の順でフォールバック
  3. スコアリング — 配合順で重み付け(先頭1/3を1.0倍・中間1/3を0.7倍・後方1/3を0.4倍)。`good`(0–3)/`caution`(0–3)の重み付き合計から`38 + Σgood×4 − Σcaution×4`を算出し5–98にクランプ
  4. 5段階グレード判定(下表)
  5. ルールベースでホンネコメント・良い点・注意点を自動生成
- **出力**: スコア・グレード・ホンネコメント・成分ごとの解説・検出タグ
- **連動**: 肌質プロフィール登録済みならパーソナルメモを追加。検出タグに応じ類似成分のプチプラ候補を自動表示

| Score | Rank | ラベル | コメント |
|---|---|---|---|
| 85–98 | S | 実力派 | 広告抜きで良い処方 |
| 70–84 | A | 優等生 | しっかり中身のある処方 |
| 55–69 | B | 堅実 | 派手さはないけど悪くない |
| 40–54 | C | 雰囲気重視 | 中身より使用感・イメージ寄り |
| 5–39 | D | 広告の勝利 | その価格、イメージ料かも |

### 5.2 コスパ診断
- **目的**: 価格のうち「中身」相当額と「ブランド料」相当額を分離して可視化
- **入力**: カテゴリ(12種:化粧水・乳液・美容液・クリーム・クレンジング・洗顔料・日焼け止め・シートマスク・シャンプー・ヘアオイル・ヘアミルク・ヘアマスク)、価格、容量
- **処理**: 単価=価格÷容量。相場ラインとの比率で5段階判定(〜1倍「超堅実」/〜2倍「適正圏内」/〜4倍「ちょい贅沢」/〜8倍「ブランド料しっかり」/それ以上「ほぼイメージ課金」)。ブランド料=`max(0, 価格−相場ライン×容量)`。ハーゲンダッツ換算=`floor(ブランド料÷350円)`
- **出力**: 判定タイトル・単価・相場倍率ゲージ・推定ブランド料・ハーゲンダッツ個数

### 5.3 マイポーチ相性チェック
- **目的**: 手持ちコスメの組み合わせのNG・注意・好相性を診断し、朝夜の使い分けを提案
- **入力**: アイテム名・カテゴリ(10種、うちヘア用3種)・配合成分(貼り付けで自動判定 or クイックタグ選択)
- **処理**: 登録アイテムを全ペア総当りで11件の相性ルールと照合。ルーティンは成分タグから朝(UVフィルター・ピュアビタミンC)/夜(レチノール・酸)/いつでもに自動振り分け
- **出力**: NG→注意→好相性順の相性カード、朝/夜/いつでものルーティン、アイテムごとのジェネリック候補
- **永続化**: `localStorage: cosme-honne-pouch`

### 5.4 天気連動アドバイス
- **目的**: 今日・今週の気象条件から先回りしたスキンケア提案
- **入力**: なし(自動)。位置情報許可があれば現在地、拒否/4秒タイムアウトで東京にフォールバック
- **処理**: Open-Meteoから現在気象+7日間予報を取得。当日アドバイスは湿度<40%/>75%、UV指数3段階、気温≥30℃/≤5℃で判定。週間先読みは「湿度急落」「UVピーク」「寒暖差」の3条件で生成
- **出力**: 今日のアドバイス、7日間ストリップ、週間先読み(最大3件)

### 5.5 ジェネリック提案
- **目的**: 高価格帯コスメと同系統成分を持つプチプラ実在品を提示
- **入力**: 成分タグ配列、カテゴリ(任意)、ドメイン(肌/髪)
- **処理**: 実在品DB(27件、参考価格つき)からタグ一致数+同カテゴリ加点(0.5)でスコアリングし上位4件を抽出。髪ドメインはタグが薄いためカテゴリ一致のみでも候補に含める
- **出力**: 商品名・ブランド・価格/容量・一致タグ・ひとことポイント
- **表示箇所**: 成分ホンネ分析の結果画面、マイポーチの各アイテム展開時

### 5.6 肌質・髪質プロフィール
- **目的**: 肌質/髪質・悩みを登録し、パーソナライズされた成分キーワードと商品を提示
- **入力**: 肌タブ(肌質5種×悩み5種)、髪タブ(髪質4種×悩み5種)
- **処理**: 各カテゴリ内商品をタイプ一致(+2点)+悩み一致数でスコアリングし上位2件を抽出
- **出力**: 推し成分チップ、カテゴリ別おすすめ、髪タブ限定「オイル vs ミルク」解説
- **連動**: 成分ホンネ分析ページに「あなた向けメモ」を自動挿入
- **永続化**: `localStorage: cosme-honne-profile`

### 5.7 成分組み合わせ事典
- **目的**: 主要成分同士の相性を常設の早見表として参照可能にする
- **処理**: 相性ルール(11件)から主要10成分の対称マトリクス(10×10)を自動生成
- **出力**: 相性マトリクス表、ルール詳細カード一覧

## 6. データモデル

| 型 | 概要 | 件数 |
|---|---|---|
| `IngredientInfo` | 成分名・別名・カテゴリ・説明・実力(good 0–3)・刺激性(caution 0–3)・タグ | 約110 |
| `ActiveTag` | 判定ロジックが参照する成分カテゴリのユニオン型 | 26種 |
| `CompatRule` | タグ同士の相性ルール(NG/注意/好相性) | 11 |
| `ProductPick / HairPick` | 実在プチプラ商品DB | 27 |
| `PouchItem` | マイポーチ登録アイテム(名前・カテゴリ・タグ) | — |
| `ProfileState` | 肌質・悩み・髪質・髪悩み | — |

## 7. 状態管理・永続化

| Store | State | localStorage key |
|---|---|---|
| `pouchStore` | `items: PouchItem[]` | `cosme-honne-pouch` |
| `profileStore` | `skinType, concerns, hairType, hairConcerns` | `cosme-honne-profile` |

## 8. デザイン仕様 — Apple風デザインシステム

Apple Human Interface Guidelinesの3原則(明瞭性・敬意・奥行き)を援用し、**色分けではなく、タイポグラフィと余白、陰影で情報階層を作る**方向に統一する。

**原則**
- Clarity(明瞭性):情報階層は色よりも文字サイズ・太さ・余白で作る。装飾的な色分けは最小限に
- Deference(敬意):UIはコンテンツを支える背景に徹する。半透明とぼかし(blur)でナビゲーションを浮かせ、主役はコンテンツにする
- Depth(奥行き):色付きの太い枠線ではなく、微細な陰影とレイヤーの重なりで階層を表現する

**カラートークン**(ブランドのコーラルピンクは維持し、「単一アクセント+ニュートラルな地」の運用に統一)

| トークン | 用途 | Light | Dark |
|---|---|---|---|
| `--background` | 画面の地 | `#F5F5F7` | `#000000` |
| `--surface` | カード・シート | `#FFFFFF` | `#1C1C1E` |
| `--label` | 本文テキスト | `#1D1D1F` | `#F5F5F7` |
| `--label-secondary` | 補助テキスト | `#6E6E73` | `#98989D` |
| `--separator` | 罫線・区切り | `rgba(60,60,67,.16)` | `rgba(84,84,88,.6)` |
| `--accent` | ブランド(単色運用) | `#E23D6B` | `#FF6482` |

セマンティックカラー(成功・警告・危険)はブランドアクセントとは別レーンとし、状態表示にのみ使う。

**タイポグラフィ**:Webフォントは読み込まず、OSネイティブのSan Francisco/ヒラギノにフォールバックさせる。
`-apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text", "Hiragino Sans", "Noto Sans JP", sans-serif`

**スペーシング・角丸**:8ptグリッド(8/12/16/20/24/32/40px)基準。角丸はsm 8px(バッジ・チップ)/ md 14px(ボタン・入力)/ lg 20px(カード)/ full(ピル・タブ)の4段階に固定。

**コンポーネントパターン**
- カード:白背景+1pxセパレーター(色付き枠線は廃止)+ごく薄い陰影。状態表示時のみセマンティックカラーの背景ティントを使う
- ボタン:プライマリはアクセント塗り+白文字、角丸14px、押下時にopacity/scaleで軽いフィードバック
- タブバー:半透明+背景ぼかし(`backdrop-filter: blur`)で固定。選択タブのみアクセントカラー
- バッジ/チップ:角丸フル、セマンティック背景ティント10%+同色文字
- 入力欄:背景をわずかに沈めたグレー、フォーカス時にアクセントカラーの2pxリング

**モーション**:150–250ms・ease-outの控えめなトランジションのみ。ページ遷移・タブ切替・展開/折りたたみに限定し、装飾目的のアニメーションは避ける。`prefers-reduced-motion`を尊重する。

**アクセシビリティ**:本文コントラスト比4.5:1以上、タップ領域44×44pt以上、キーボードフォーカスリング常時可視化、絵文字には意味の伝わるラベルテキストを併記。

**現状との差分**
- 現行実装:暖色クリーム背景+コーラルピンク基調、セクションごとに多色パステルの枠線(ピンク/アンバー/エメラルド/バイオレット)、角丸基準が画面ごとに揺れている
- 本仕様の方向:地はニュートラルグレー、アクセントはコーラルピンク一色に統一。色分けではなく余白と陰影で階層を作り、角丸・スペーシングを上記スケールに揃える

## 9. 非対応・既知の制約

- 医療行為ではない。皮膚科医の診断や処方薬の代替にはならない
- 成分辞書(約110件)・商品DB(27件)は代表的なものに限定。未掲載の成分・商品は判定対象外、またはパターン推定止まり
- 価格は参考価格であり、実売価格とは異なる場合がある
- 永続化は`localStorage`のみのため、ブラウザ変更・端末変更でデータは引き継がれない
- 天気取得はOpen-Meteoに依存。位置情報が許可されない場合は東京の値で代替

## 10. 今後の展望

- JAN/バーコードスキャンによる商品特定(依存ライブラリ`@zxing`の再活用)
- 成分辞書・商品DBの拡充、外部データソースとの連携
- 任意アカウント連携によるクラウド同期・マルチデバイス対応
- 8章で定義したApple風デザインシステムのコード実装への反映
